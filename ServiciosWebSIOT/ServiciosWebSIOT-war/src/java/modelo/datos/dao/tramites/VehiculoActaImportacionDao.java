package modelo.datos.dao.tramites;

import java.sql.*;
import java.util.*;
import java.math.*;

import modelo.datos.dao.NotFoundException;
import modelo.datos.objetos.tramites.VehiculoActaImportacion;


/**
  * VehiculoActaImportacion Data Access Object (DAO) migrado a firebird 2.5.
  * This class contains all database handling that is needed to
  * permanently store and retrieve VehiculoActaImportacion object instances.
  */

 /**
  * This sourcecode has been generated by FREE DaoGen generator version 2.4.1.
  * The usage of generated code is restricted to OpenSource software projects
  * only. DaoGen is available in http://titaniclinux.net/daogen/
  * It has been programmed by Tuomo Lukka, Tuomo.Lukka@iki.fi
  *
  * DaoGen license: The following DaoGen generated source code is licensed
  * under the terms of GNU GPL license. The full text for license is available
  * in GNU project's pages: http://www.gnu.org/copyleft/gpl.html
  *
  * If you wish to use the DaoGen generator to produce code for closed-source
  * commercial applications, you must pay the lisence fee. The price is
  * 5 USD or 5 Eur for each database table, you are generating code for.
  * (That includes unlimited amount of iterations with all supported languages
  * for each database table you are paying for.) Send mail to
  * "Tuomo.Lukka@iki.fi" for more information. Thank you!
  */



public class VehiculoActaImportacionDao {



    /**
     * createValueObject-method. This method is used when the Dao class needs
     * to create new value object instance. The reason why this method exists
     * is that sometimes the programmer may want to extend also the valueObject
     * and then this method can be overrided to return extended valueObject.
     * NOTE: If you extend the valueObject class, make sure to override the
     * clone() method in it!
     */
    public VehiculoActaImportacion createValueObject() {
          return new VehiculoActaImportacion();
    }


    /**
     * getObject-method. This will create and load valueObject contents from database 
     * using given Primary-Key as identifier. This method is just a convenience method 
     * for the real load-method which accepts the valueObject as a parameter. Returned
     * valueObject will be created using the createValueObject() method.
     */
    public VehiculoActaImportacion getObject(Connection conn, int ID_VEHICULO) throws NotFoundException, SQLException {

          VehiculoActaImportacion valueObject = createValueObject();
          valueObject.setID_VEHICULO(ID_VEHICULO);
          load(conn, valueObject);
          return valueObject;
    }


    /**
     * load-method. This will load valueObject contents from database using
     * Primary-Key as identifier. Upper layer should use this so that valueObject
     * instance is created and only primary-key should be specified. Then call
     * this method to complete other persistent information. This method will
     * overwrite all other fields except primary-key and possible runtime variables.
     * If load can not find matching row, NotFoundException will be thrown.
     *
     * @param conn         This method requires working database connection.
     * @param valueObject  This parameter contains the class instance to be loaded.
     *                     Primary-key field must be set for this to work properly.
     */
    public void load(Connection conn, VehiculoActaImportacion valueObject) throws NotFoundException, SQLException {

          String sql = "SELECT * FROM VEHICULOACTAIMPORTACION WHERE (ID_VEHICULO = ? ) "; 
          PreparedStatement stmt = null;

          try {
               stmt = conn.prepareStatement(sql);
               stmt.setInt(1, valueObject.getID_VEHICULO()); 

               singleQuery(conn, stmt, valueObject);

          } finally {
              //if (stmt != null)
              //    stmt.close();
          }
    }


    /**
     * LoadAll-method. This will read all contents from database table and
     * build a List containing valueObjects. Please note, that this method
     * will consume huge amounts of resources if table has lot's of rows. 
     * This should only be used when target tables have only small amounts
     * of data.
     *
     * @param conn         This method requires working database connection.
     */
    public List loadAll(Connection conn) throws SQLException {

          String sql = "SELECT * FROM VEHICULOACTAIMPORTACION ORDER BY ID_VEHICULO ASC ";
          List searchResults = listQuery(conn, conn.prepareStatement(sql));

          return searchResults;
    }



    /**
     * create-method. This will create new row in database according to supplied
     * valueObject contents. Make sure that values for all NOT NULL columns are
     * correctly specified. Also, if this table does not use automatic surrogate-keys
     * the primary-key must be specified. After INSERT command this method will 
     * read the generated primary-key back to valueObject if automatic surrogate-keys
     * were used. 
     *
     * @param conn         This method requires working database connection.
     * @param valueObject  This parameter contains the class instance to be created.
     *                     If automatic surrogate-keys are not used the Primary-key 
     *                     field must be set for this to work properly.
     */
    public synchronized void create(Connection conn, VehiculoActaImportacion valueObject) throws SQLException {

          String sql = "";
          PreparedStatement stmt = null;
          ResultSet result = null;

          try {
               sql = "INSERT INTO VEHICULOACTAIMPORTACION ( ID_VEHICULO, FICHATECNICAHOMOLOGA, SERIE_MOTOR, "
               + "CHASIS, NRO_MOTOR, NUMEROVIN, "
               + "ID_CVEHICULO, ID_TCARROCERIA, NRO_PUERTAS, "
               + "CAPACIDAD, CILINDRAJE, ID_COMBUSTIBLE, "
               + "POTENCIA, NRO_EJES, PESO_BRUTO, "
               + "MODELO, ID_MARCAPPAL, ID_LINEAPPAL, "
               + "ID_MARCA, ID_LINEA, CARGADO, ID_PAIS) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?) ";
               stmt = conn.prepareStatement(sql);

               stmt.setInt(1, valueObject.getID_VEHICULO()); 
               stmt.setString(2, valueObject.getFICHATECNICAHOMOLOGA()); 
               stmt.setString(3, valueObject.getSERIE_MOTOR()); 
               stmt.setString(4, valueObject.getCHASIS()); 
               stmt.setString(5, valueObject.getNRO_MOTOR()); 
               stmt.setString(6, valueObject.getNUMEROVIN()); 
               stmt.setInt(7, valueObject.getID_CVEHICULO()); 
               stmt.setInt(8, valueObject.getID_TCARROCERIA()); 
               stmt.setInt(9, valueObject.getNRO_PUERTAS()); 
               stmt.setString(10, valueObject.getCAPACIDAD()); 
               stmt.setString(11, valueObject.getCILINDRAJE()); 
               stmt.setInt(12, valueObject.getID_COMBUSTIBLE()); 
               stmt.setString(13, valueObject.getPOTENCIA()); 
               stmt.setInt(14, valueObject.getNRO_EJES()); 
               stmt.setString(15, valueObject.getPESO_BRUTO()); 
               stmt.setInt(16, valueObject.getMODELO()); 
               stmt.setInt(17, valueObject.getID_MARCAPPAL()); 
               stmt.setInt(18, valueObject.getID_LINEAPPAL()); 
               stmt.setString(19, valueObject.getID_MARCA()); 
               stmt.setInt(20, valueObject.getID_LINEA()); 
               stmt.setString(21, valueObject.getCARGADO()); 
               stmt.setInt(22, valueObject.getID_PAIS());

               int rowcount = databaseUpdate(conn, stmt);
               if (rowcount != 1) {
                    //System.out.println("PrimaryKey Error when updating DB!");
                    throw new SQLException("PrimaryKey Error when updating DB!");
               }

          } finally {
              if (stmt != null)
                  stmt.close();
          }


    }


    /**
     * save-method. This method will save the current state of valueObject to database.
     * Save can not be used to create new instances in database, so upper layer must
     * make sure that the primary-key is correctly specified. Primary-key will indicate
     * which instance is going to be updated in database. If save can not find matching 
     * row, NotFoundException will be thrown.
     *
     * @param conn         This method requires working database connection.
     * @param valueObject  This parameter contains the class instance to be saved.
     *                     Primary-key field must be set for this to work properly.
     */
    public void save(Connection conn, VehiculoActaImportacion valueObject) 
          throws NotFoundException, SQLException {

          String sql = "UPDATE VEHICULOACTAIMPORTACION SET FICHATECNICAHOMOLOGA = ?, SERIE_MOTOR = ?, CHASIS = ?, "
               + "NRO_MOTOR = ?, NUMEROVIN = ?, ID_CVEHICULO = ?, "
               + "ID_TCARROCERIA = ?, NRO_PUERTAS = ?, CAPACIDAD = ?, "
               + "CILINDRAJE = ?, ID_COMBUSTIBLE = ?, POTENCIA = ?, "
               + "NRO_EJES = ?, PESO_BRUTO = ?, MODELO = ?, "
               + "ID_MARCAPPAL = ?, ID_LINEAPPAL = ?, ID_MARCA = ?, "
               + "ID_LINEA = ?, CARGADO = ?, ID_PAIS = ? WHERE (ID_VEHICULO = ? ) ";
          PreparedStatement stmt = null;

          try {
              stmt = conn.prepareStatement(sql);
              stmt.setString(1, valueObject.getFICHATECNICAHOMOLOGA()); 
              stmt.setString(2, valueObject.getSERIE_MOTOR()); 
              stmt.setString(3, valueObject.getCHASIS()); 
              stmt.setString(4, valueObject.getNRO_MOTOR()); 
              stmt.setString(5, valueObject.getNUMEROVIN()); 
              stmt.setInt(6, valueObject.getID_CVEHICULO()); 
              stmt.setInt(7, valueObject.getID_TCARROCERIA()); 
              stmt.setInt(8, valueObject.getNRO_PUERTAS()); 
              stmt.setString(9, valueObject.getCAPACIDAD()); 
              stmt.setString(10, valueObject.getCILINDRAJE()); 
              stmt.setInt(11, valueObject.getID_COMBUSTIBLE()); 
              stmt.setString(12, valueObject.getPOTENCIA()); 
              stmt.setInt(13, valueObject.getNRO_EJES()); 
              stmt.setString(14, valueObject.getPESO_BRUTO()); 
              stmt.setInt(15, valueObject.getMODELO()); 
              stmt.setInt(16, valueObject.getID_MARCAPPAL()); 
              stmt.setInt(17, valueObject.getID_LINEAPPAL()); 
              stmt.setString(18, valueObject.getID_MARCA()); 
              stmt.setInt(19, valueObject.getID_LINEA()); 
              stmt.setString(20, valueObject.getCARGADO()); 
              stmt.setInt(21, valueObject.getID_PAIS());
              

              stmt.setInt(22, valueObject.getID_VEHICULO()); 

              int rowcount = databaseUpdate(conn, stmt);
              if (rowcount == 0) {
                   //System.out.println("Object could not be saved! (PrimaryKey not found)");
                   throw new NotFoundException("Object could not be saved! (PrimaryKey not found)");
              }
              if (rowcount > 1) {
                   //System.out.println("PrimaryKey Error when updating DB! (Many objects were affected!)");
                   throw new SQLException("PrimaryKey Error when updating DB! (Many objects were affected!)");
              }
          } finally {
              if (stmt != null)
                  stmt.close();
          }
    }


    /**
     * delete-method. This method will remove the information from database as identified by
     * by primary-key in supplied valueObject. Once valueObject has been deleted it can not 
     * be restored by calling save. Restoring can only be done using create method but if 
     * database is using automatic surrogate-keys, the resulting object will have different 
     * primary-key than what it was in the deleted object. If delete can not find matching row,
     * NotFoundException will be thrown.
     *
     * @param conn         This method requires working database connection.
     * @param valueObject  This parameter contains the class instance to be deleted.
     *                     Primary-key field must be set for this to work properly.
     */
    public void delete(Connection conn, VehiculoActaImportacion valueObject) 
          throws NotFoundException, SQLException {

          String sql = "DELETE FROM VEHICULOACTAIMPORTACION WHERE (ID_VEHICULO = ? ) ";
          PreparedStatement stmt = null;

          try {
              stmt = conn.prepareStatement(sql);
              stmt.setInt(1, valueObject.getID_VEHICULO()); 

              int rowcount = databaseUpdate(conn, stmt);
              if (rowcount == 0) {
                   //System.out.println("Object could not be deleted (PrimaryKey not found)");
                   throw new NotFoundException("Object could not be deleted! (PrimaryKey not found)");
              }
              if (rowcount > 1) {
                   //System.out.println("PrimaryKey Error when updating DB! (Many objects were deleted!)");
                   throw new SQLException("PrimaryKey Error when updating DB! (Many objects were deleted!)");
              }
          } finally {
              if (stmt != null)
                  stmt.close();
          }
    }


    /**
     * deleteAll-method. This method will remove all information from the table that matches
     * this Dao and ValueObject couple. This should be the most efficient way to clear table.
     * Once deleteAll has been called, no valueObject that has been created before can be 
     * restored by calling save. Restoring can only be done using create method but if database 
     * is using automatic surrogate-keys, the resulting object will have different primary-key 
     * than what it was in the deleted object. (Note, the implementation of this method should
     * be different with different DB backends.)
     *
     * @param conn         This method requires working database connection.
     */
    public void deleteAll(Connection conn) throws SQLException {

          String sql = "DELETE FROM VEHICULOACTAIMPORTACION";
          PreparedStatement stmt = null;

          try {
              stmt = conn.prepareStatement(sql);
              int rowcount = databaseUpdate(conn, stmt);
          } finally {
              if (stmt != null)
                  stmt.close();
          }
    }


    /**
     * coutAll-method. This method will return the number of all rows from table that matches
     * this Dao. The implementation will simply execute "select count(primarykey) from table".
     * If table is empty, the return value is 0. This method should be used before calling
     * loadAll, to make sure table has not too many rows.
     *
     * @param conn         This method requires working database connection.
     */
    public int countAll(Connection conn) throws SQLException {

          String sql = "SELECT count(*) FROM VEHICULOACTAIMPORTACION";
          PreparedStatement stmt = null;
          ResultSet result = null;
          int allRows = 0;

          try {
              stmt = conn.prepareStatement(sql);
              result = stmt.executeQuery();

              if (result.next())
                  allRows = result.getInt(1);
          } finally {
              if (result != null)
                  result.close();
              if (stmt != null)
                  stmt.close();
          }
          return allRows;
    }


    /** 
     * searchMatching-Method. This method provides searching capability to 
     * get matching valueObjects from database. It works by searching all 
     * objects that match permanent instance variables of given object.
     * Upper layer should use this by setting some parameters in valueObject
     * and then  call searchMatching. The result will be 0-N objects in a List, 
     * all matching those criteria you specified. Those instance-variables that
     * have NULL values are excluded in search-criteria.
     *
     * @param conn         This method requires working database connection.
     * @param valueObject  This parameter contains the class instance where search will be based.
     *                     Primary-key field should not be set.
     */
    public List searchMatching(Connection conn, VehiculoActaImportacion valueObject) throws SQLException {

          List searchResults;

          boolean first = true;
          StringBuffer sql = new StringBuffer("SELECT * FROM VEHICULOACTAIMPORTACION WHERE 1=1 ");

          if (valueObject.getID_VEHICULO() != 0) {
              if (first) { first = false; }
              sql.append("AND ID_VEHICULO = ").append(valueObject.getID_VEHICULO()).append(" ");
          }

          if (valueObject.getFICHATECNICAHOMOLOGA() != null) {
              if (first) { first = false; }
              sql.append("AND FICHATECNICAHOMOLOGA LIKE '").append(valueObject.getFICHATECNICAHOMOLOGA()).append("%' ");
          }

          if (valueObject.getSERIE_MOTOR() != null) {
              if (first) { first = false; }
              sql.append("AND SERIE_MOTOR LIKE '").append(valueObject.getSERIE_MOTOR()).append("%' ");
          }

          if (valueObject.getCHASIS() != null) {
              if (first) { first = false; }
              sql.append("AND CHASIS LIKE '").append(valueObject.getCHASIS()).append("%' ");
          }

          if (valueObject.getNRO_MOTOR() != null) {
              if (first) { first = false; }
              sql.append("AND NRO_MOTOR LIKE '").append(valueObject.getNRO_MOTOR()).append("%' ");
          }

          if (valueObject.getNUMEROVIN() != null) {
              if (first) { first = false; }
              sql.append("AND NUMEROVIN LIKE '").append(valueObject.getNUMEROVIN()).append("%' ");
          }

          if (valueObject.getID_CVEHICULO() != 0) {
              if (first) { first = false; }
              sql.append("AND ID_CVEHICULO = ").append(valueObject.getID_CVEHICULO()).append(" ");
          }

          if (valueObject.getID_TCARROCERIA() != 0) {
              if (first) { first = false; }
              sql.append("AND ID_TCARROCERIA = ").append(valueObject.getID_TCARROCERIA()).append(" ");
          }

          if (valueObject.getNRO_PUERTAS() != 0) {
              if (first) { first = false; }
              sql.append("AND NRO_PUERTAS = ").append(valueObject.getNRO_PUERTAS()).append(" ");
          }

          if (valueObject.getCAPACIDAD() != null) {
              if (first) { first = false; }
              sql.append("AND CAPACIDAD LIKE '").append(valueObject.getCAPACIDAD()).append("%' ");
          }

          if (valueObject.getCILINDRAJE() != null) {
              if (first) { first = false; }
              sql.append("AND CILINDRAJE LIKE '").append(valueObject.getCILINDRAJE()).append("%' ");
          }

          if (valueObject.getID_COMBUSTIBLE() != 0) {
              if (first) { first = false; }
              sql.append("AND ID_COMBUSTIBLE = ").append(valueObject.getID_COMBUSTIBLE()).append(" ");
          }

          if (valueObject.getPOTENCIA() != null) {
              if (first) { first = false; }
              sql.append("AND POTENCIA LIKE '").append(valueObject.getPOTENCIA()).append("%' ");
          }

          if (valueObject.getNRO_EJES() != 0) {
              if (first) { first = false; }
              sql.append("AND NRO_EJES = ").append(valueObject.getNRO_EJES()).append(" ");
          }

          if (valueObject.getPESO_BRUTO() != null) {
              if (first) { first = false; }
              sql.append("AND PESO_BRUTO LIKE '").append(valueObject.getPESO_BRUTO()).append("%' ");
          }

          if (valueObject.getMODELO() != 0) {
              if (first) { first = false; }
              sql.append("AND MODELO = ").append(valueObject.getMODELO()).append(" ");
          }

          if (valueObject.getID_MARCAPPAL() != 0) {
              if (first) { first = false; }
              sql.append("AND ID_MARCAPPAL = ").append(valueObject.getID_MARCAPPAL()).append(" ");
          }

          if (valueObject.getID_LINEAPPAL() != 0) {
              if (first) { first = false; }
              sql.append("AND ID_LINEAPPAL = ").append(valueObject.getID_LINEAPPAL()).append(" ");
          }

          if (valueObject.getID_MARCA() != null) {
              if (first) { first = false; }
              sql.append("AND ID_MARCA LIKE '").append(valueObject.getID_MARCA()).append("%' ");
          }

          if (valueObject.getID_LINEA() != 0) {
              if (first) { first = false; }
              sql.append("AND ID_LINEA = ").append(valueObject.getID_LINEA()).append(" ");
          }

          if (valueObject.getCARGADO() != null) {
              if (first) { first = false; }
              sql.append("AND CARGADO LIKE '").append(valueObject.getCARGADO()).append("%' ");
          }
          if (valueObject.getID_PAIS() != 0) {
              if (first) { first = false; }
              sql.append("AND ID_PAIS = ").append(valueObject.getID_PAIS()).append(" ");
          }


          sql.append("ORDER BY ID_VEHICULO ASC ");

          // Prevent accidential full table results.
          // Use loadAll if all rows must be returned.
          if (first)
               searchResults = new ArrayList();
          else
               searchResults = listQuery(conn, conn.prepareStatement(sql.toString()));

          return searchResults;
    }


    /** 
     * getDaogenVersion will return information about
     * generator which created these sources.
     */
    public String getDaogenVersion() {
        return "DaoGen version 2.4.1";
    }


    /**
     * databaseUpdate-method. This method is a helper method for internal use. It will execute
     * all database handling that will change the information in tables. SELECT queries will
     * not be executed here however. The return value indicates how many rows were affected.
     * This method will also make sure that if cache is used, it will reset when data changes.
     *
     * @param conn         This method requires working database connection.
     * @param stmt         This parameter contains the SQL statement to be excuted.
     */
    protected int databaseUpdate(Connection conn, PreparedStatement stmt) throws SQLException {

          int result = stmt.executeUpdate();

          return result;
    }



    /**
     * databaseQuery-method. This method is a helper method for internal use. It will execute
     * all database queries that will return only one row. The resultset will be converted
     * to valueObject. If no rows were found, NotFoundException will be thrown.
     *
     * @param conn         This method requires working database connection.
     * @param stmt         This parameter contains the SQL statement to be excuted.
     * @param valueObject  Class-instance where resulting data will be stored.
     */
    protected void singleQuery(Connection conn, PreparedStatement stmt, VehiculoActaImportacion valueObject) 
          throws NotFoundException, SQLException {

          ResultSet result = null;

          try {
              result = stmt.executeQuery();

              if (result.next()) {

                   valueObject.setID_VEHICULO(result.getInt("ID_VEHICULO")); 
                   valueObject.setFICHATECNICAHOMOLOGA(result.getString("FICHATECNICAHOMOLOGA")); 
                   valueObject.setSERIE_MOTOR(result.getString("SERIE_MOTOR")); 
                   valueObject.setCHASIS(result.getString("CHASIS")); 
                   valueObject.setNRO_MOTOR(result.getString("NRO_MOTOR")); 
                   valueObject.setNUMEROVIN(result.getString("NUMEROVIN")); 
                   valueObject.setID_CVEHICULO(result.getInt("ID_CVEHICULO")); 
                   valueObject.setID_TCARROCERIA(result.getInt("ID_TCARROCERIA")); 
                   valueObject.setNRO_PUERTAS(result.getInt("NRO_PUERTAS")); 
                   valueObject.setCAPACIDAD(result.getString("CAPACIDAD")); 
                   valueObject.setCILINDRAJE(result.getString("CILINDRAJE")); 
                   valueObject.setID_COMBUSTIBLE(result.getInt("ID_COMBUSTIBLE")); 
                   valueObject.setPOTENCIA(result.getString("POTENCIA")); 
                   valueObject.setNRO_EJES(result.getInt("NRO_EJES")); 
                   valueObject.setPESO_BRUTO(result.getString("PESO_BRUTO")); 
                   valueObject.setMODELO(result.getInt("MODELO")); 
                   valueObject.setID_MARCAPPAL(result.getInt("ID_MARCAPPAL")); 
                   valueObject.setID_LINEAPPAL(result.getInt("ID_LINEAPPAL")); 
                   valueObject.setID_MARCA(result.getString("ID_MARCA")); 
                   valueObject.setID_LINEA(result.getInt("ID_LINEA")); 
                   valueObject.setCARGADO(result.getString("CARGADO")); 
                   valueObject.setID_PAIS(result.getInt("ID_PAIS"));

              } else {
                    //System.out.println("VehiculoActaImportacion Object Not Found!");
                    throw new NotFoundException("VehiculoActaImportacion Object Not Found!");
              }
          } finally {
              if (result != null)
                  result.close();
              if (stmt != null)
                  stmt.close();
          }
    }


    /**
     * databaseQuery-method. This method is a helper method for internal use. It will execute
     * all database queries that will return multiple rows. The resultset will be converted
     * to the List of valueObjects. If no rows were found, an empty List will be returned.
     *
     * @param conn         This method requires working database connection.
     * @param stmt         This parameter contains the SQL statement to be excuted.
     */
    protected List listQuery(Connection conn, PreparedStatement stmt) throws SQLException {

          ArrayList searchResults = new ArrayList();
          ResultSet result = null;

          try {
              result = stmt.executeQuery();

              while (result.next()) {
                   VehiculoActaImportacion temp = createValueObject();

                   temp.setID_VEHICULO(result.getInt("ID_VEHICULO")); 
                   temp.setFICHATECNICAHOMOLOGA(result.getString("FICHATECNICAHOMOLOGA")); 
                   temp.setSERIE_MOTOR(result.getString("SERIE_MOTOR")); 
                   temp.setCHASIS(result.getString("CHASIS")); 
                   temp.setNRO_MOTOR(result.getString("NRO_MOTOR")); 
                   temp.setNUMEROVIN(result.getString("NUMEROVIN")); 
                   temp.setID_CVEHICULO(result.getInt("ID_CVEHICULO")); 
                   temp.setID_TCARROCERIA(result.getInt("ID_TCARROCERIA")); 
                   temp.setNRO_PUERTAS(result.getInt("NRO_PUERTAS")); 
                   temp.setCAPACIDAD(result.getString("CAPACIDAD")); 
                   temp.setCILINDRAJE(result.getString("CILINDRAJE")); 
                   temp.setID_COMBUSTIBLE(result.getInt("ID_COMBUSTIBLE")); 
                   temp.setPOTENCIA(result.getString("POTENCIA")); 
                   temp.setNRO_EJES(result.getInt("NRO_EJES")); 
                   temp.setPESO_BRUTO(result.getString("PESO_BRUTO")); 
                   temp.setMODELO(result.getInt("MODELO")); 
                   temp.setID_MARCAPPAL(result.getInt("ID_MARCAPPAL")); 
                   temp.setID_LINEAPPAL(result.getInt("ID_LINEAPPAL")); 
                   temp.setID_MARCA(result.getString("ID_MARCA")); 
                   temp.setID_LINEA(result.getInt("ID_LINEA")); 
                   temp.setCARGADO(result.getString("CARGADO")); 
                   temp.setID_PAIS(result.getInt("ID_PAIS"));

                   searchResults.add(temp);
              }

          } finally {
              if (result != null)
                  result.close();
              if (stmt != null)
                  stmt.close();
          }

          return (List)searchResults;
    }
    
  public int getIdFB(Connection conn)throws SQLException{
    String sql = "SELECT GEN_ID(GEN_VEHICULOACTAIMPORTACION,1) FROM TRANSITO";
    PreparedStatement stmt = null;
    ResultSet result = null;
    int allRows = 0;
    try {
        stmt = conn.prepareStatement(sql);
        result = stmt.executeQuery();
        if (result.next())
            allRows = result.getInt(1)+1;
        } finally {
        if (result != null)
            result.close();
        if (stmt != null)
            stmt.close();
        }
    return allRows;
    }

  public void estadoVehiculo(Connection conn, int id, String estado)throws SQLException{
    String sql = "UPDATE VEHICULOACTAIMPORTACION CARGADO = ? WHERE (ID_VEHICULO = ?)";
    PreparedStatement stmt = null;
    try {
        stmt = conn.prepareStatement(sql);
        stmt.setString(1, estado);
        stmt.setInt(2, id);
        int rowcount = databaseUpdate(conn, stmt);
    }  
    
    finally {
        if (stmt != null)
            stmt.close();
    }
  }
}
