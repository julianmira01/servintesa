package modelo.datos.dao.liquidacion.caja;

import java.sql.*;

import java.util.*;

import java.math.*;

import java.text.SimpleDateFormat;

import modelo.datos.dao.NotFoundException;
import modelo.datos.objetos.liquidacion.caja.LPagosRunt;

import utilidades.Funciones;


/**
  * LPagosRunt Data Access Object (DAO) migrado a firebird 2.5.
  * This class contains all database handling that is needed to
  * permanently store and retrieve LPagosRunt object instances.
  */

/**
 * This sourcecode has been generated by FREE DaoGen generator version 2.4.1.
 * The usage of generated code is restricted to OpenSource software projects
 * only. DaoGen is available in http://titaniclinux.net/daogen/
 * It has been programmed by Tuomo Lukka, Tuomo.Lukka@iki.fi
 *
 * DaoGen license: The following DaoGen generated source code is licensed
 * under the terms of GNU GPL license. The full text for license is available
 * in GNU project's pages: http://www.gnu.org/copyleft/gpl.html
 *
 * If you wish to use the DaoGen generator to produce code for closed-source
 * commercial applications, you must pay the lisence fee. The price is
 * 5 USD or 5 Eur for each database table, you are generating code for.
 * (That includes unlimited amount of iterations with all supported languages
 * for each database table you are paying for.) Send mail to
 * "Tuomo.Lukka@iki.fi" for more information. Thank you!
 */


public class LPagosRuntDao {


    /**
     * createValueObject-method. This method is used when the Dao class needs
     * to create new value object instance. The reason why this method exists
     * is that sometimes the programmer may want to extend also the valueObject
     * and then this method can be overrided to return extended valueObject.
     * NOTE: If you extend the valueObject class, make sure to override the
     * clone() method in it!
     */
    public LPagosRunt createValueObject() {
        return new LPagosRunt();
    }


    /**
     * getObject-method. This will create and load valueObject contents from database
     * using given Primary-Key as identifier. This method is just a convenience method
     * for the real load-method which accepts the valueObject as a parameter. Returned
     * valueObject will be created using the createValueObject() method.
     */
    public LPagosRunt getObject(Connection conn, int ID_PAGORUNT) throws NotFoundException, SQLException {

        LPagosRunt valueObject = createValueObject();
        valueObject.setID_PAGORUNT(ID_PAGORUNT);
        load(conn, valueObject);
        return valueObject;
    }


    /**
     * load-method. This will load valueObject contents from database using
     * Primary-Key as identifier. Upper layer should use this so that valueObject
     * instance is created and only primary-key should be specified. Then call
     * this method to complete other persistent information. This method will
     * overwrite all other fields except primary-key and possible runtime variables.
     * If load can not find matching row, NotFoundException will be thrown.
     *
     * @param conn         This method requires working database connection.
     * @param valueObject  This parameter contains the class instance to be loaded.
     *                     Primary-key field must be set for this to work properly.
     */
    public void load(Connection conn, LPagosRunt valueObject) throws NotFoundException, SQLException {

        String sql = "SELECT * FROM L_PAGOSRUNT WHERE (ID_PAGORUNT = ? ) ";
        PreparedStatement stmt = null;

        try {
            stmt = conn.prepareStatement(sql);
            stmt.setInt(1, valueObject.getID_PAGORUNT());

            singleQuery(conn, stmt, valueObject);

        } finally {
            //if (stmt != null)
            //    stmt.close();
        }
    }


    /**
     * LoadAll-method. This will read all contents from database table and
     * build a List containing valueObjects. Please note, that this method
     * will consume huge amounts of resources if table has lot's of rows.
     * This should only be used when target tables have only small amounts
     * of data.
     *
     * @param conn         This method requires working database connection.
     */
    public List loadAll(Connection conn) throws SQLException {

        String sql = "SELECT * FROM L_PAGOSRUNT ORDER BY ID_PAGORUNT ASC ";
        List searchResults = listQuery(conn, conn.prepareStatement(sql));

        return searchResults;
    }


    /**
     * create-method. This will create new row in database according to supplied
     * valueObject contents. Make sure that values for all NOT NULL columns are
     * correctly specified. Also, if this table does not use automatic surrogate-keys
     * the primary-key must be specified. After INSERT command this method will
     * read the generated primary-key back to valueObject if automatic surrogate-keys
     * were used.
     *
     * @param conn         This method requires working database connection.
     * @param valueObject  This parameter contains the class instance to be created.
     *                     If automatic surrogate-keys are not used the Primary-key
     *                     field must be set for this to work properly.
     */
    public synchronized void create(Connection conn, LPagosRunt valueObject) throws SQLException {

        String sql = "";
        PreparedStatement stmt = null;
        ResultSet result = null;

        try {
            sql =
"INSERT INTO L_PAGOSRUNT ( ID_PAGORUNT, ID_FACTURA, FECHASOLICITUD, " + "FECHAREGISTRO, FECHAVIGENCIA, TIPOLIQUIDACION, " +
  "TIPOCOMPROBANTEPAGO, VALORMT, VALORRUNT, " + "VALORTOTAL, ESTADOLIQUIDACION, FECHAPAGO, " +
  "ID_BANCO, ID_TIPOMONEDA, NUMAUTORIZACIONBANCO, " +
  "ID_USRPAGO, ID_ARQUEO, NUMLIQUIDACION) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?) ";
            stmt = conn.prepareStatement(sql);

            stmt.setInt(1, valueObject.getID_PAGORUNT());

            if (!Funciones.EnteroesNulo(valueObject.getID_FACTURA()))
                stmt.setInt(2, valueObject.getID_FACTURA());
            else
                stmt.setNull(2, Types.INTEGER);


            if (Funciones.esFecha(valueObject.getFECHASOLICITUD()))
                stmt.setDate(3, Funciones.convFechaSSQL(valueObject.getFECHASOLICITUD()));
            else
                stmt.setNull(3, Types.DATE);

            if (Funciones.esFecha(valueObject.getFECHAREGISTRO()))
                stmt.setDate(4, Funciones.convFechaSSQL(valueObject.getFECHAREGISTRO()));
            else
                stmt.setNull(4, Types.DATE);

            if (Funciones.esFecha(valueObject.getFECHAVIGENCIA()))
                stmt.setDate(5, Funciones.convFechaSSQL(valueObject.getFECHAVIGENCIA()));
            else
                stmt.setNull(5, Types.DATE);

            if (valueObject.getTIPOLIQUIDACION() != null && !valueObject.getTIPOLIQUIDACION().equals(""))
                stmt.setString(6, valueObject.getTIPOLIQUIDACION());
            else
                stmt.setNull(6, Types.VARCHAR);

            if (valueObject.getTIPOCOMPROBANTEPAGO() != null && !valueObject.getTIPOCOMPROBANTEPAGO().equals(""))
                stmt.setString(7, valueObject.getTIPOCOMPROBANTEPAGO());
            else
                stmt.setNull(7, Types.VARCHAR);

            if (valueObject.getVALORMT() != null && !valueObject.getVALORMT().equals(""))
                stmt.setString(8, valueObject.getVALORMT());
            else
                stmt.setNull(8, Types.DOUBLE);

            if (valueObject.getVALORRUNT() != null && !valueObject.getVALORRUNT().equals(""))
                stmt.setString(9, valueObject.getVALORRUNT());
            else
                stmt.setNull(9, Types.DOUBLE);

            if (valueObject.getVALORTOTAL() != null && !valueObject.getVALORTOTAL().equals(""))
                stmt.setString(10, valueObject.getVALORTOTAL());
            else
                stmt.setNull(10, Types.DOUBLE);

            if (valueObject.getESTADOLIQUIDACION() != null && !valueObject.getESTADOLIQUIDACION().equals(""))
                stmt.setString(11, valueObject.getESTADOLIQUIDACION());
            else
                stmt.setNull(11, Types.VARCHAR);

            if (Funciones.esFecha(valueObject.getFECHAPAGO()))
                stmt.setDate(12, Funciones.convFechaSSQL(valueObject.getFECHAPAGO()));
            else
                stmt.setNull(12, Types.DATE);

            if (!Funciones.EnteroesNulo(valueObject.getID_BANCO()))
                stmt.setInt(13, valueObject.getID_BANCO());
            else
                stmt.setNull(13, Types.INTEGER);

            if (!Funciones.EnteroesNulo(valueObject.getID_TIPOMONEDA()))
                stmt.setInt(14, valueObject.getID_TIPOMONEDA());
            else
                stmt.setNull(14, Types.INTEGER);

            if (valueObject.getNUMAUTORIZACIONBANCO() != null && !valueObject.getNUMAUTORIZACIONBANCO().equals(""))
                stmt.setString(15, valueObject.getNUMAUTORIZACIONBANCO());
            else
                stmt.setNull(15, Types.VARCHAR);

            if (!Funciones.EnteroesNulo(valueObject.getID_USRPAGO()))
                stmt.setInt(16, valueObject.getID_USRPAGO());
            else
                stmt.setNull(16, Types.INTEGER);

            if (!Funciones.EnteroesNulo(valueObject.getID_ARQUEO()))
                stmt.setInt(17, valueObject.getID_ARQUEO());
            else
                stmt.setNull(17, Types.INTEGER);

            if (valueObject.getNUMLIQUIDACION() != null && !valueObject.getNUMLIQUIDACION().equals(""))
                stmt.setString(18, valueObject.getNUMLIQUIDACION());
            else
                stmt.setNull(17, Types.VARCHAR);

            int rowcount = databaseUpdate(conn, stmt);
            if (rowcount != 1) {
                //System.out.println("PrimaryKey Error when updating DB!");
                throw new SQLException("PrimaryKey Error when updating DB!");
            }

        } finally {
            if (stmt != null)
                stmt.close();
        }


    }


    /**
     * save-method. This method will save the current state of valueObject to database.
     * Save can not be used to create new instances in database, so upper layer must
     * make sure that the primary-key is correctly specified. Primary-key will indicate
     * which instance is going to be updated in database. If save can not find matching
     * row, NotFoundException will be thrown.
     *
     * @param conn         This method requires working database connection.
     * @param valueObject  This parameter contains the class instance to be saved.
     *                     Primary-key field must be set for this to work properly.
     */
    
    /*
    UPDATE l_pagosrunt
    set l_pagosrunt.id_factura =
    (select l_facturas.lf_id from l_facturas where l_facturas.lf_numero='10130524144');
    where l_pagosrunt.numliquidacion = '11' 
     */
    
    
    public void actualizarId_Factura(Connection conn,String numfacturalocal,String numfacturarunt) throws NotFoundException, SQLException
    {
        String sql = "UPDATE l_pagosrunt " + 
        "set l_pagosrunt.id_factura = " + 
        "(select l_facturas.lf_id from l_facturas where l_facturas.lf_numero = ?) " + 
        "where l_pagosrunt.numliquidacion = ? ";
        
        PreparedStatement stmt = null;
        
        try
        {
            
            stmt = conn.prepareStatement(sql);
            
            stmt.setString(1, numfacturalocal);   
            stmt.setString(2, numfacturarunt);
            
            databaseUpdate(conn, stmt);
            
        
        } finally {
            if (stmt != null)
                stmt.close();
        }
        
    }
    
    public void save(Connection conn, LPagosRunt valueObject) throws NotFoundException, SQLException {

        String sql =
            "UPDATE L_PAGOSRUNT SET ID_FACTURA = ?, FECHASOLICITUD = ?, FECHAREGISTRO = ?, " + "FECHAVIGENCIA = ?, TIPOLIQUIDACION = ?, TIPOCOMPROBANTEPAGO = ?, " +
            "VALORMT = ?, VALORRUNT = ?, VALORTOTAL = ?, " + "ESTADOLIQUIDACION = ?, FECHAPAGO = ?, ID_BANCO = ?, " +
            "ID_TIPOMONEDA = ?, NUMAUTORIZACIONBANCO = ?, ID_USRPAGO = ?, " +
            "ID_ARQUEO = ?, NUMLIQUIDACION = ?  WHERE (ID_PAGORUNT = ? ) ";
        PreparedStatement stmt = null;

        try {
            stmt = conn.prepareStatement(sql);

            stmt.setInt(1, valueObject.getID_FACTURA());

            if (Funciones.esFecha(valueObject.getFECHASOLICITUD()))
                stmt.setDate(2, Funciones.convFechaSSQL(valueObject.getFECHASOLICITUD()));
            else
                stmt.setNull(2, Types.DATE);

            if (Funciones.esFecha(valueObject.getFECHAREGISTRO()))
                stmt.setDate(3, Funciones.convFechaSSQL(valueObject.getFECHAREGISTRO()));
            else
                stmt.setNull(3, Types.DATE);

            if (Funciones.esFecha(valueObject.getFECHAVIGENCIA()))
                stmt.setDate(4, Funciones.convFechaSSQL(valueObject.getFECHAVIGENCIA()));
            else
                stmt.setNull(4, Types.DATE);

            if (Funciones.contarCadena(valueObject.getTIPOLIQUIDACION(), 20))
                stmt.setString(5, valueObject.getTIPOLIQUIDACION());
            else
                stmt.setNull(5, Types.VARCHAR);

            if (Funciones.contarCadena(valueObject.getTIPOCOMPROBANTEPAGO(), 20))
                stmt.setString(6, valueObject.getTIPOCOMPROBANTEPAGO());
            else
                stmt.setNull(6, Types.VARCHAR);

            if (Funciones.contarCadena(valueObject.getVALORMT(), 20))
                stmt.setString(7, valueObject.getVALORMT());
            else
                stmt.setNull(7, Types.VARCHAR);

            if (Funciones.contarCadena(valueObject.getVALORRUNT(), 20))
                stmt.setString(8, valueObject.getVALORRUNT());
            else
                stmt.setNull(8, Types.VARCHAR);

            if (Funciones.contarCadena(valueObject.getVALORTOTAL(), 20))
                stmt.setString(9, valueObject.getVALORTOTAL());
            else
                stmt.setNull(9, Types.VARCHAR);

            if (Funciones.contarCadena(valueObject.getVALORTOTAL(), 20))
                stmt.setString(10, valueObject.getVALORTOTAL());
            else
                stmt.setNull(10, Types.VARCHAR);

            if (Funciones.esFecha(valueObject.getFECHAPAGO()))
                stmt.setDate(11, Funciones.convFechaSSQL(valueObject.getFECHAPAGO()));
            else
                stmt.setNull(11, Types.DATE);

            if (valueObject.getID_BANCO() == 0)
                stmt.setNull(12, valueObject.getID_BANCO());
            else if (!Funciones.EnteroesNulo(valueObject.getID_BANCO()))
                stmt.setInt(12, valueObject.getID_BANCO());
            else
                stmt.setNull(12, Types.INTEGER);

            if (valueObject.getID_BANCO() == 0)
                stmt.setNull(13, valueObject.getID_TIPOMONEDA());
            else if (!Funciones.EnteroesNulo(valueObject.getID_TIPOMONEDA()))
                stmt.setInt(13, valueObject.getID_TIPOMONEDA());
            else
                stmt.setNull(13, Types.INTEGER);

            if (Funciones.contarCadena(valueObject.getNUMAUTORIZACIONBANCO(), 50))
                stmt.setString(14, valueObject.getNUMAUTORIZACIONBANCO());
            else
                stmt.setNull(14, Types.VARCHAR);

            if (valueObject.getID_USRPAGO() == 0)
                stmt.setNull(15, valueObject.getID_USRPAGO());
            else if (!Funciones.EnteroesNulo(valueObject.getID_USRPAGO()))
                stmt.setInt(15, valueObject.getID_USRPAGO());
            else
                stmt.setNull(15, Types.INTEGER);

            if (valueObject.getID_ARQUEO() == 0)
                stmt.setNull(16, valueObject.getID_ARQUEO());
            else if (!Funciones.EnteroesNulo(valueObject.getID_ARQUEO()))
                stmt.setInt(16, valueObject.getID_ARQUEO());
            else
                stmt.setNull(16, Types.INTEGER);


            if (Funciones.contarCadena(valueObject.getNUMLIQUIDACION(), 30))
                stmt.setString(17, valueObject.getNUMLIQUIDACION());
            else
                stmt.setNull(17, Types.VARCHAR);


            if (valueObject.getID_PAGORUNT() == 0)
                stmt.setNull(18, valueObject.getID_PAGORUNT());
            else if (!Funciones.EnteroesNulo(valueObject.getID_PAGORUNT()))
                stmt.setInt(18, valueObject.getID_PAGORUNT());
            else
                stmt.setNull(17, Types.INTEGER);

            int rowcount = databaseUpdate(conn, stmt);
            if (rowcount == 0) {
                //System.out.println("Object could not be saved! (PrimaryKey not found)");
                throw new NotFoundException("Object could not be saved! (PrimaryKey not found)");
            }
            if (rowcount > 1) {
                //System.out.println("PrimaryKey Error when updating DB! (Many objects were affected!)");
                throw new SQLException("PrimaryKey Error when updating DB! (Many objects were affected!)");
            }
        } finally {
            if (stmt != null)
                stmt.close();
        }
    }


    /**
     * delete-method. This method will remove the information from database as identified by
     * by primary-key in supplied valueObject. Once valueObject has been deleted it can not
     * be restored by calling save. Restoring can only be done using create method but if
     * database is using automatic surrogate-keys, the resulting object will have different
     * primary-key than what it was in the deleted object. If delete can not find matching row,
     * NotFoundException will be thrown.
     *
     * @param conn         This method requires working database connection.
     * @param valueObject  This parameter contains the class instance to be deleted.
     *                     Primary-key field must be set for this to work properly.
     */
    public void delete(Connection conn, LPagosRunt valueObject) throws NotFoundException, SQLException {

        String sql = "DELETE FROM L_PAGOSRUNT WHERE (ID_PAGORUNT = ? ) ";
        PreparedStatement stmt = null;

        try {
            stmt = conn.prepareStatement(sql);
            stmt.setInt(1, valueObject.getID_PAGORUNT());

            int rowcount = databaseUpdate(conn, stmt);
            if (rowcount == 0) {
                //System.out.println("Object could not be deleted (PrimaryKey not found)");
                throw new NotFoundException("Object could not be deleted! (PrimaryKey not found)");
            }
            if (rowcount > 1) {
                //System.out.println("PrimaryKey Error when updating DB! (Many objects were deleted!)");
                throw new SQLException("PrimaryKey Error when updating DB! (Many objects were deleted!)");
            }
        } finally {
            if (stmt != null)
                stmt.close();
        }
    }


    /**
     * deleteAll-method. This method will remove all information from the table that matches
     * this Dao and ValueObject couple. This should be the most efficient way to clear table.
     * Once deleteAll has been called, no valueObject that has been created before can be
     * restored by calling save. Restoring can only be done using create method but if database
     * is using automatic surrogate-keys, the resulting object will have different primary-key
     * than what it was in the deleted object. (Note, the implementation of this method should
     * be different with different DB backends.)
     *
     * @param conn         This method requires working database connection.
     */
    public void deleteAll(Connection conn) throws SQLException {

        String sql = "DELETE FROM L_PAGOSRUNT";
        PreparedStatement stmt = null;

        try {
            stmt = conn.prepareStatement(sql);
            int rowcount = databaseUpdate(conn, stmt);
        } finally {
            if (stmt != null)
                stmt.close();
        }
    }


    /**
     * coutAll-method. This method will return the number of all rows from table that matches
     * this Dao. The implementation will simply execute "select count(primarykey) from table".
     * If table is empty, the return value is 0. This method should be used before calling
     * loadAll, to make sure table has not too many rows.
     *
     * @param conn         This method requires working database connection.
     */
    public int countAll(Connection conn) throws SQLException {

        String sql = "SELECT count(*) FROM L_PAGOSRUNT";
        PreparedStatement stmt = null;
        ResultSet result = null;
        int allRows = 0;

        try {
            stmt = conn.prepareStatement(sql);
            result = stmt.executeQuery();

            if (result.next())
                allRows = result.getInt(1);
        } finally {
            if (result != null)
                result.close();
            if (stmt != null)
                stmt.close();
        }
        return allRows;
    }


    /**
     * searchMatching-Method. This method provides searching capability to
     * get matching valueObjects from database. It works by searching all
     * objects that match permanent instance variables of given object.
     * Upper layer should use this by setting some parameters in valueObject
     * and then  call searchMatching. The result will be 0-N objects in a List,
     * all matching those criteria you specified. Those instance-variables that
     * have NULL values are excluded in search-criteria.
     *
     * @param conn         This method requires working database connection.
     * @param valueObject  This parameter contains the class instance where search will be based.
     *                     Primary-key field should not be set.
     */
    public List searchMatching(Connection conn, LPagosRunt valueObject) throws SQLException {

        List searchResults;

        boolean first = true;
        StringBuffer sql = new StringBuffer("SELECT * FROM L_PAGOSRUNT WHERE 1=1 ");

        if (valueObject.getID_PAGORUNT() != 0) {
            if (first) {
                first = false;
            }
            sql.append("AND ID_PAGORUNT = ").append(valueObject.getID_PAGORUNT()).append(" ");
        }

        if (valueObject.getID_FACTURA() != 0) {
            if (first) {
                first = false;
            }
            sql.append("AND ID_FACTURA = ").append(valueObject.getID_FACTURA()).append(" ");
        }

        if (valueObject.getFECHASOLICITUD() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND FECHASOLICITUD = '").append(valueObject.getFECHASOLICITUD()).append("%' ");
        }

        if (valueObject.getFECHAREGISTRO() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND FECHAREGISTRO = '").append(valueObject.getFECHAREGISTRO()).append("%' ");
        }

        if (valueObject.getFECHAVIGENCIA() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND FECHAVIGENCIA = '").append(valueObject.getFECHAVIGENCIA()).append("%' ");
        }

        if (valueObject.getTIPOLIQUIDACION() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND TIPOLIQUIDACION = '").append(valueObject.getTIPOLIQUIDACION()).append("%' ");
        }

        if (valueObject.getTIPOCOMPROBANTEPAGO() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND TIPOCOMPROBANTEPAGO = '").append(valueObject.getTIPOCOMPROBANTEPAGO()).append("%' ");
        }

        if (valueObject.getVALORMT() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND VALORMT = '").append(valueObject.getVALORMT()).append("%' ");
        }

        if (valueObject.getVALORRUNT() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND VALORRUNT = '").append(valueObject.getVALORRUNT()).append("%' ");
        }

        if (valueObject.getVALORTOTAL() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND VALORTOTAL = '").append(valueObject.getVALORTOTAL()).append("%' ");
        }

        if (valueObject.getESTADOLIQUIDACION() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND ESTADOLIQUIDACION = '").append(valueObject.getESTADOLIQUIDACION()).append("%' ");
        }

        if (valueObject.getFECHAPAGO() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND FECHAPAGO = '").append(valueObject.getFECHAPAGO()).append("%' ");
        }

        if (valueObject.getID_BANCO() != 0) {
            if (first) {
                first = false;
            }
            sql.append("AND ID_BANCO = ").append(valueObject.getID_BANCO()).append(" ");
        }

        if (valueObject.getID_TIPOMONEDA() != 0) {
            if (first) {
                first = false;
            }
            sql.append("AND ID_TIPOMONEDA = ").append(valueObject.getID_TIPOMONEDA()).append(" ");
        }

        if (valueObject.getNUMAUTORIZACIONBANCO() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND NUMAUTORIZACIONBANCO = '").append(valueObject.getNUMAUTORIZACIONBANCO()).append("%' ");
        }

        if (valueObject.getID_USRPAGO() != 0) {
            if (first) {
                first = false;
            }
            sql.append("AND ID_USRPAGO = ").append(valueObject.getID_USRPAGO()).append(" ");
        }

        if (valueObject.getID_ARQUEO() != 0) {
            if (first) {
                first = false;
            }
            sql.append("AND ID_ARQUEO = ").append(valueObject.getID_ARQUEO()).append(" ");
        }

        if (valueObject.getNUMLIQUIDACION() != null) {
            if (first) {
                first = false;
            }
            sql.append("AND NUMLIQUIDACION = '").append(valueObject.getNUMLIQUIDACION()).append("' ");
        }


        sql.append("ORDER BY ID_PAGORUNT ASC ");

        // Prevent accidential full table results.
        // Use loadAll if all rows must be returned.
        if (first)
            searchResults = new ArrayList();
        else
            searchResults = listQuery(conn, conn.prepareStatement(sql.toString()));

        return searchResults;
    }


    /**
     * getDaogenVersion will return information about
     * generator which created these sources.
     */
    public String getDaogenVersion() {
        return "DaoGen version 2.4.1";
    }


    /**
     * databaseUpdate-method. This method is a helper method for internal use. It will execute
     * all database handling that will change the information in tables. SELECT queries will
     * not be executed here however. The return value indicates how many rows were affected.
     * This method will also make sure that if cache is used, it will reset when data changes.
     *
     * @param conn         This method requires working database connection.
     * @param stmt         This parameter contains the SQL statement to be excuted.
     */
    protected int databaseUpdate(Connection conn, PreparedStatement stmt) throws SQLException {

        int result = stmt.executeUpdate();

        return result;
    }


    /**
     * databaseQuery-method. This method is a helper method for internal use. It will execute
     * all database queries that will return only one row. The resultset will be converted
     * to valueObject. If no rows were found, NotFoundException will be thrown.
     *
     * @param conn         This method requires working database connection.
     * @param stmt         This parameter contains the SQL statement to be excuted.
     * @param valueObject  Class-instance where resulting data will be stored.
     */
    protected void singleQuery(Connection conn, PreparedStatement stmt,
                               LPagosRunt valueObject) throws NotFoundException, SQLException {

        ResultSet result = null;

        try {
            result = stmt.executeQuery();

            if (result.next()) {

                valueObject.setID_PAGORUNT(result.getInt("ID_PAGORUNT"));
                valueObject.setID_FACTURA(result.getInt("ID_FACTURA"));
                valueObject.setFECHASOLICITUD(result.getString("FECHASOLICITUD"));
                valueObject.setFECHAREGISTRO(result.getString("FECHAREGISTRO"));
                valueObject.setFECHAVIGENCIA(result.getString("FECHAVIGENCIA"));
                valueObject.setTIPOLIQUIDACION(result.getString("TIPOLIQUIDACION"));
                valueObject.setTIPOCOMPROBANTEPAGO(result.getString("TIPOCOMPROBANTEPAGO"));
                valueObject.setVALORMT(result.getString("VALORMT"));
                valueObject.setVALORRUNT(result.getString("VALORRUNT"));
                valueObject.setVALORTOTAL(result.getString("VALORTOTAL"));
                valueObject.setESTADOLIQUIDACION(result.getString("ESTADOLIQUIDACION"));
                valueObject.setFECHAPAGO(result.getString("FECHAPAGO"));
                valueObject.setID_BANCO(result.getInt("ID_BANCO"));
                valueObject.setID_TIPOMONEDA(result.getInt("ID_TIPOMONEDA"));
                valueObject.setNUMAUTORIZACIONBANCO(result.getString("NUMAUTORIZACIONBANCO"));
                valueObject.setID_USRPAGO(result.getInt("ID_USRPAGO"));
                valueObject.setID_ARQUEO(result.getInt("ID_ARQUEO"));
                valueObject.setNUMLIQUIDACION(result.getString("NUMLIQUIDACION"));

            } else {
                //System.out.println("LPagosRunt Object Not Found!");
                throw new NotFoundException("LPagosRunt Object Not Found!");
            }
        } finally {
            if (result != null)
                result.close();
            if (stmt != null)
                stmt.close();
        }
    }


    /**
     * databaseQuery-method. This method is a helper method for internal use. It will execute
     * all database queries that will return multiple rows. The resultset will be converted
     * to the List of valueObjects. If no rows were found, an empty List will be returned.
     *
     * @param conn         This method requires working database connection.
     * @param stmt         This parameter contains the SQL statement to be excuted.
     */
    protected List listQuery(Connection conn, PreparedStatement stmt) throws SQLException {

        ArrayList searchResults = new ArrayList();
        ResultSet result = null;

        try {
            result = stmt.executeQuery();

            while (result.next()) {
                LPagosRunt temp = createValueObject();

                temp.setID_PAGORUNT(result.getInt("ID_PAGORUNT"));
                temp.setID_FACTURA(result.getInt("ID_FACTURA"));
                temp.setFECHASOLICITUD(result.getString("FECHASOLICITUD"));
                temp.setFECHAREGISTRO(result.getString("FECHAREGISTRO"));
                temp.setFECHAVIGENCIA(result.getString("FECHAVIGENCIA"));
                temp.setTIPOLIQUIDACION(result.getString("TIPOLIQUIDACION"));
                temp.setTIPOCOMPROBANTEPAGO(result.getString("TIPOCOMPROBANTEPAGO"));
                temp.setVALORMT(result.getString("VALORMT"));
                temp.setVALORRUNT(result.getString("VALORRUNT"));
                temp.setVALORTOTAL(result.getString("VALORTOTAL"));
                temp.setESTADOLIQUIDACION(result.getString("ESTADOLIQUIDACION"));
                temp.setFECHAPAGO(result.getString("FECHAPAGO"));
                temp.setID_BANCO(result.getInt("ID_BANCO"));
                temp.setID_TIPOMONEDA(result.getInt("ID_TIPOMONEDA"));
                temp.setNUMAUTORIZACIONBANCO(result.getString("NUMAUTORIZACIONBANCO"));
                temp.setID_USRPAGO(result.getInt("ID_USRPAGO"));
                temp.setID_ARQUEO(result.getInt("ID_ARQUEO"));
                temp.setNUMLIQUIDACION(result.getString("NUMLIQUIDACION"));

                searchResults.add(temp);
            }

        } finally {
            if (result != null)
                result.close();
            if (stmt != null)
                stmt.close();
        }

        return (List)searchResults;
    }

    public String getMaxFecha(Connection conn, int idcajero) throws SQLException {

        String sql =
            "SELECT max(l_pagosrunt.fechapago) fecha from l_pagosrunt where l_pagosrunt.id_usrpago= " + idcajero +
            " and l_pagosrunt.id_arqueo Is  Null";
        PreparedStatement stmt = null;
        ResultSet result = null;
        String maxfecha = null;
        SimpleDateFormat formato = new SimpleDateFormat("dd/MM/yyyy");
        try {
            stmt = conn.prepareStatement(sql);
            result = stmt.executeQuery();

            if (result.next())
                maxfecha = formato.format(result.getDate(1));
        } finally {
            if (result != null)
                result.close();
            if (stmt != null)
                stmt.close();
        }

        System.out.println(sql);

        return maxfecha;
    }

    public String getMinFecha(Connection conn, int idcajero) throws SQLException {

        String sql =
            "SELECT min(l_pagosrunt.fechapago) fecha from l_pagosrunt where l_pagosrunt.id_usrpago= " + idcajero +
            " and l_pagosrunt.id_arqueo Is  Null";
        PreparedStatement stmt = null;
        ResultSet result = null;
        String minfecha = null;
        SimpleDateFormat formato = new SimpleDateFormat("dd/MM/yyyy");
        try {
            stmt = conn.prepareStatement(sql);
            result = stmt.executeQuery();

            if (result.next())
                minfecha = formato.format(result.getDate(1));
        } finally {
            if (result != null)
                result.close();
            if (stmt != null)
                stmt.close();
        }

        return minfecha;
    }

}
