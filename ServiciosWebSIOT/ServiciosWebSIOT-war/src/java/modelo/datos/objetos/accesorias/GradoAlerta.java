package modelo.datos.objetos.accesorias;

import java.io.*;
import java.sql.*;
import java.util.*;
import java.math.*;

 /**
  * GradoAlerta Value Object.
  * This class is value object representing database table RUNT_GRADOALERTA
  * This class is intented to be used together with associated Dao object.
  */

 /**
  * This sourcecode has been generated by FREE DaoGen generator version 2.4.1.
  * The usage of generated code is restricted to OpenSource software projects
  * only. DaoGen is available in http://titaniclinux.net/daogen/
  * It has been programmed by Tuomo Lukka, Tuomo.Lukka@iki.fi
  *
  * DaoGen license: The following DaoGen generated source code is licensed
  * under the terms of GNU GPL license. The full text for license is available
  * in GNU project's pages: http://www.gnu.org/copyleft/gpl.html
  *
  * If you wish to use the DaoGen generator to produce code for closed-source
  * commercial applications, you must pay the lisence fee. The price is
  * 5 USD or 5 Eur for each database table, you are generating code for.
  * (That includes unlimited amount of iterations with all supported languages
  * for each database table you are paying for.) Send mail to
  * "Tuomo.Lukka@iki.fi" for more information. Thank you!
  */



public class GradoAlerta implements Cloneable, Serializable {

    /** 
     * Persistent Instance variables. This data is directly 
     * mapped to the columns of database table.
     */
    private int ID_GRADOALERTA;
    private String COD_GRADOALERTA;
    private String DES_GRADOALERTA;
    private String IDRUNT;



    /** 
     * Constructors. DaoGen generates two constructors by default.
     * The first one takes no arguments and provides the most simple
     * way to create object instance. The another one takes one
     * argument, which is the primary key of the corresponding table.
     */

    public GradoAlerta () {

    }

    public GradoAlerta (int ID_GRADOALERTAIn) {

          this.ID_GRADOALERTA = ID_GRADOALERTAIn;

    }


    /** 
     * Get- and Set-methods for persistent variables. The default
     * behaviour does not make any checks against malformed data,
     * so these might require some manual additions.
     */

    public int getID_GRADOALERTA() {
          return this.ID_GRADOALERTA;
    }
    public void setID_GRADOALERTA(int ID_GRADOALERTAIn) {
          this.ID_GRADOALERTA = ID_GRADOALERTAIn;
    }

    public String getCOD_GRADOALERTA() {
          return this.COD_GRADOALERTA;
    }
    public void setCOD_GRADOALERTA(String COD_GRADOALERTAIn) {
          this.COD_GRADOALERTA = COD_GRADOALERTAIn;
    }

    public String getDES_GRADOALERTA() {
          return this.DES_GRADOALERTA;
    }
    public void setDES_GRADOALERTA(String DES_GRADOALERTAIn) {
          this.DES_GRADOALERTA = DES_GRADOALERTAIn;
    }



    /** 
     * setAll allows to set all persistent variables in one method call.
     * This is useful, when all data is available and it is needed to 
     * set the initial state of this object. Note that this method will
     * directly modify instance variales, without going trough the 
     * individual set-methods.
     */

    public void setAll(int ID_GRADOALERTAIn,
          String COD_GRADOALERTAIn,
          String DES_GRADOALERTAIn) {
          this.ID_GRADOALERTA = ID_GRADOALERTAIn;
          this.COD_GRADOALERTA = COD_GRADOALERTAIn;
          this.DES_GRADOALERTA = DES_GRADOALERTAIn;
    }


    /** 
     * hasEqualMapping-method will compare two GradoAlerta instances
     * and return true if they contain same values in all persistent instance 
     * variables. If hasEqualMapping returns true, it does not mean the objects
     * are the same instance. However it does mean that in that moment, they 
     * are mapped to the same row in database.
     */
    public boolean hasEqualMapping(GradoAlerta valueObject) {

          if (valueObject.getID_GRADOALERTA() != this.ID_GRADOALERTA) {
                    return(false);
          }
          if (this.COD_GRADOALERTA == null) {
                    if (valueObject.getCOD_GRADOALERTA() != null)
                           return(false);
          } else if (!this.COD_GRADOALERTA.equals(valueObject.getCOD_GRADOALERTA())) {
                    return(false);
          }
          if (this.DES_GRADOALERTA == null) {
                    if (valueObject.getDES_GRADOALERTA() != null)
                           return(false);
          } else if (!this.DES_GRADOALERTA.equals(valueObject.getDES_GRADOALERTA())) {
                    return(false);
          }

          return true;
    }



    /**
     * toString will return String object representing the state of this 
     * valueObject. This is useful during application development, and 
     * possibly when application is writing object states in textlog.
     */
    public String toString() {
        StringBuffer out = new StringBuffer(this.getDaogenVersion());
        out.append("\nclass GradoAlerta, mapping to table RUNT_GRADOALERTA\n");
        out.append("Persistent attributes: \n"); 
        out.append("ID_GRADOALERTA = " + this.ID_GRADOALERTA + "\n"); 
        out.append("COD_GRADOALERTA = " + this.COD_GRADOALERTA + "\n"); 
        out.append("DES_GRADOALERTA = " + this.DES_GRADOALERTA + "\n"); 
        return out.toString();
    }


    /**
     * Clone will return identical deep copy of this valueObject.
     * Note, that this method is different than the clone() which
     * is defined in java.lang.Object. Here, the retuned cloned object
     * will also have all its attributes cloned.
     */
    public Object clone() {
        GradoAlerta cloned = new GradoAlerta();

        cloned.setID_GRADOALERTA(this.ID_GRADOALERTA); 
        if (this.COD_GRADOALERTA != null)
             cloned.setCOD_GRADOALERTA(new String(this.COD_GRADOALERTA)); 
        if (this.DES_GRADOALERTA != null)
             cloned.setDES_GRADOALERTA(new String(this.DES_GRADOALERTA)); 
        return cloned;
    }



    /** 
     * getDaogenVersion will return information about
     * generator which created these sources.
     */
    public String getDaogenVersion() {
        return "DaoGen version 2.4.1";
    }

    public String getIDRUNT() {
        return IDRUNT;
    }

    public void setIDRUNT(String IDRUNT) {
        this.IDRUNT = IDRUNT;
    }
}
