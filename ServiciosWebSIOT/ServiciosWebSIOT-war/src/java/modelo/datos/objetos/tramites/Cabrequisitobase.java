package modelo.datos.objetos.tramites;

import java.io.*;
import java.sql.*;
import java.util.*;
import java.math.*;

 /**
  * Cabrequisitobase Value Object.
  * This class is value object representing database table CABREQUISITOBASE
  * This class is intented to be used together with associated Dao object.
  */

 /**
  * This sourcecode has been generated by FREE DaoGen generator version 2.4.1.
  * The usage of generated code is restricted to OpenSource software projects
  * only. DaoGen is available in http://titaniclinux.net/daogen/
  * It has been programmed by Tuomo Lukka, Tuomo.Lukka@iki.fi
  *
  * DaoGen license: The following DaoGen generated source code is licensed
  * under the terms of GNU GPL license. The full text for license is available
  * in GNU project's pages: http://www.gnu.org/copyleft/gpl.html
  *
  * If you wish to use the DaoGen generator to produce code for closed-source
  * commercial applications, you must pay the lisence fee. The price is
  * 5 USD or 5 Eur for each database table, you are generating code for.
  * (That includes unlimited amount of iterations with all supported languages
  * for each database table you are paying for.) Send mail to
  * "Tuomo.Lukka@iki.fi" for more information. Thank you!
  */



public class Cabrequisitobase implements Cloneable, Serializable {

    /** 
     * Persistent Instance variables. This data is directly 
     * mapped to the columns of database table.
     */
    private int ID_CABREQBASE;
    private String COD_CABREQBASE;
    private String DESC_CABREQBASE;
    private String ACTIVO;
    private int ID_TIPO_BASE;



    /** 
     * Constructors. DaoGen generates two constructors by default.
     * The first one takes no arguments and provides the most simple
     * way to create object instance. The another one takes one
     * argument, which is the primary key of the corresponding table.
     */

    public Cabrequisitobase () {

    }

    public Cabrequisitobase (int ID_CABREQBASEIn) {

          this.ID_CABREQBASE = ID_CABREQBASEIn;

    }


    /** 
     * Get- and Set-methods for persistent variables. The default
     * behaviour does not make any checks against malformed data,
     * so these might require some manual additions.
     */

    public int getID_CABREQBASE() {
          return this.ID_CABREQBASE;
    }
    public void setID_CABREQBASE(int ID_CABREQBASEIn) {
          this.ID_CABREQBASE = ID_CABREQBASEIn;
    }

    public String getCOD_CABREQBASE() {
          return this.COD_CABREQBASE;
    }
    public void setCOD_CABREQBASE(String COD_CABREQBASEIn) {
          this.COD_CABREQBASE = COD_CABREQBASEIn;
    }

    public String getDESC_CABREQBASE() {
          return this.DESC_CABREQBASE;
    }
    public void setDESC_CABREQBASE(String DESC_CABREQBASEIn) {
          this.DESC_CABREQBASE = DESC_CABREQBASEIn;
    }

    public String getACTIVO() {
          return this.ACTIVO;
    }
    public void setACTIVO(String ACTIVOIn) {
          this.ACTIVO = ACTIVOIn;
    }

    public int getID_TIPO_BASE() {
          return this.ID_TIPO_BASE;
    }
    public void setID_TIPO_BASE(int ID_TIPO_BASEIn) {
          this.ID_TIPO_BASE = ID_TIPO_BASEIn;
    }



    /** 
     * setAll allows to set all persistent variables in one method call.
     * This is useful, when all data is available and it is needed to 
     * set the initial state of this object. Note that this method will
     * directly modify instance variales, without going trough the 
     * individual set-methods.
     */

    public void setAll(int ID_CABREQBASEIn,
          String COD_CABREQBASEIn,
          String DESC_CABREQBASEIn,
          String ACTIVOIn,
          int ID_TIPO_BASEIn) {
          this.ID_CABREQBASE = ID_CABREQBASEIn;
          this.COD_CABREQBASE = COD_CABREQBASEIn;
          this.DESC_CABREQBASE = DESC_CABREQBASEIn;
          this.ACTIVO = ACTIVOIn;
          this.ID_TIPO_BASE = ID_TIPO_BASEIn;
    }


    /** 
     * hasEqualMapping-method will compare two Cabrequisitobase instances
     * and return true if they contain same values in all persistent instance 
     * variables. If hasEqualMapping returns true, it does not mean the objects
     * are the same instance. However it does mean that in that moment, they 
     * are mapped to the same row in database.
     */
    public boolean hasEqualMapping(Cabrequisitobase valueObject) {

          if (valueObject.getID_CABREQBASE() != this.ID_CABREQBASE) {
                    return(false);
          }
          if (this.COD_CABREQBASE == null) {
                    if (valueObject.getCOD_CABREQBASE() != null)
                           return(false);
          } else if (!this.COD_CABREQBASE.equals(valueObject.getCOD_CABREQBASE())) {
                    return(false);
          }
          if (this.DESC_CABREQBASE == null) {
                    if (valueObject.getDESC_CABREQBASE() != null)
                           return(false);
          } else if (!this.DESC_CABREQBASE.equals(valueObject.getDESC_CABREQBASE())) {
                    return(false);
          }
          if (this.ACTIVO == null) {
                    if (valueObject.getACTIVO() != null)
                           return(false);
          } else if (!this.ACTIVO.equals(valueObject.getACTIVO())) {
                    return(false);
          }
          if (valueObject.getID_TIPO_BASE() != this.ID_TIPO_BASE) {
                    return(false);
          }

          return true;
    }



    /**
     * toString will return String object representing the state of this 
     * valueObject. This is useful during application development, and 
     * possibly when application is writing object states in textlog.
     */
    public String toString() {
        StringBuffer out = new StringBuffer(this.getDaogenVersion());
        out.append("\nclass Cabrequisitobase, mapping to table CABREQUISITOBASE\n");
        out.append("Persistent attributes: \n"); 
        out.append("ID_CABREQBASE = " + this.ID_CABREQBASE + "\n"); 
        out.append("COD_CABREQBASE = " + this.COD_CABREQBASE + "\n"); 
        out.append("DESC_CABREQBASE = " + this.DESC_CABREQBASE + "\n"); 
        out.append("ACTIVO = " + this.ACTIVO + "\n"); 
        out.append("ID_TIPO_BASE = " + this.ID_TIPO_BASE + "\n"); 
        return out.toString();
    }


    /**
     * Clone will return identical deep copy of this valueObject.
     * Note, that this method is different than the clone() which
     * is defined in java.lang.Object. Here, the retuned cloned object
     * will also have all its attributes cloned.
     */
    public Object clone() {
        Cabrequisitobase cloned = new Cabrequisitobase();

        cloned.setID_CABREQBASE(this.ID_CABREQBASE); 
        if (this.COD_CABREQBASE != null)
             cloned.setCOD_CABREQBASE(new String(this.COD_CABREQBASE)); 
        if (this.DESC_CABREQBASE != null)
             cloned.setDESC_CABREQBASE(new String(this.DESC_CABREQBASE)); 
        if (this.ACTIVO != null)
             cloned.setACTIVO(new String(this.ACTIVO)); 
        cloned.setID_TIPO_BASE(this.ID_TIPO_BASE); 
        return cloned;
    }



    /** 
     * getDaogenVersion will return information about
     * generator which created these sources.
     */
    public String getDaogenVersion() {
        return "DaoGen version 2.4.1";
    }

}

